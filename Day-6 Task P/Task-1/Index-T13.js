let message;
let lock = 0;
//here we lock the value to zero it let the value to false statement and print else part. 
//Dont change any code below this
if (lock && " " || undefined )
{
message = "Go away";
}
else
{
message = "welcome";
}
console.log(message);